// As tests-thresholds.config, but with a stricter minimum depth, to show the
// filter changing what survives.
docker.enabled = true
docker.runOptions = '-u $(id -u):$(id -g)'

process {
    withName: 'BCFTOOLS_.*' {
        container = 'ghcr.io/eit-gbi/nf-mod-bcftools:latest'
        cpus      = 1
    }
    withName: BCFTOOLS_CALL {
        ext.args  = '-q 0 -a AD,DP'
        ext.args2 = '--ploidy 1'
        ext.args3 = "-e 'FMT/DP<2 || QUAL<5'"
    }
}
